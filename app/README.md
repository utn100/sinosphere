# Sinosphere — Flutter App

See the [root README](../README.md) for full documentation.

## Quick start

```bash
# 1. Copy the generated database (see root README for pipeline instructions)
cp ../data/db/sinosphere.db assets/sinosphere.db

# 2. Install dependencies
flutter pub get

# 3. Build
flutter build apk --release
```
